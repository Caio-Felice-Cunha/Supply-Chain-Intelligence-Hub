# Development and test dependencies.
# Install with: pip install -r requirements-dev.txt
#
# The test suite under tests/ runs fully offline against synthetic DataFrames
# and only needs the analytics core (no MySQL, Docker, or network).
pandas>=2.1.4
numpy>=1.26.0
scipy>=1.11.4
scikit-learn>=1.3.2
sqlalchemy>=2.0.23
pytest>=7.4.0
