# Define the services that will run together
services:
  # ============================================
  # MySQL Database Service
  # ============================================
  mysql:
    build:
      context: .
      dockerfile: docker/Dockerfile.mysql
    container_name: supply-chain-mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-local-root-only}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-supply_chain_db}
      MYSQL_USER: ${MYSQL_USER:-analytics_user}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-local-demo-only}
    ports:
      - "3307:3306"  # Expose on host localhost:3307 (container port 3306)
    volumes:
      - mysql_data:/var/lib/mysql  # Persistent data storage
      - ./sql:/docker-entrypoint-initdb.d  # Auto-run SQL scripts on startup
    networks:
      - analytics-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
      interval: 10s

  # ============================================
  # Jupyter Notebook Service (Python + R)
  # ============================================
  jupyter:
    build:
      context: .
      dockerfile: docker/Dockerfile.jupyter
    container_name: supply-chain-jupyter
    depends_on:
      mysql:
        condition: service_healthy
    ports:
      - "8889:8888"
    volumes:
      - ./notebooks/python:/home/jovyan/work/notebooks
      - ./data:/home/jovyan/work/data
      - ./scripts:/home/jovyan/work/scripts
    environment:
      JUPYTER_ENABLE_LAB: "yes"
      NB_UID: 1000
      NB_GID: 100
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USER: ${MYSQL_USER:-analytics_user}
      DB_PASSWORD: ${MYSQL_PASSWORD:-local-demo-only}
      DB_NAME: ${MYSQL_DATABASE:-supply_chain_db}
    networks:
      - analytics-network
    command: ["jupyter", "lab", "--ip=0.0.0.0", "--port=8888", "--no-browser", "--IdentityProvider.token=analytics"]

networks:
  analytics-network:
    driver: bridge

volumes:
  mysql_data:
    driver: local
